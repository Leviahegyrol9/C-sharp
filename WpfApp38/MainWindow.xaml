<Window x:Class="WpfApp38.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfApp38"
        mc:Ignorable="d"
        Title="Tanulói értékelés" Height="300" Width="400">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Név -->
        <Label Content="Tanuló neve:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding StudentName, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="0" Grid.Column="1" Margin="5" Width="180"/>

        <!-- Pontszám -->
        <Label Content="Elért pontszám:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding Score, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="1" Grid.Column="1" Margin="5" Width="180"/>

        <!-- Értékelés gomb -->
        <Button Content="Értékelés"
                Command="{Binding EvaluateCommand}"
                Grid.Row="2" Grid.ColumnSpan="2"
                Width="100"
                Height="30"
                Margin="5"
                HorizontalAlignment="Center"/>

        <!-- Eredmények -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="5">
            <TextBlock Text="{Binding GradeText}"
                       FontWeight="Bold"
                       Margin="0,10,0,10"/>

            <TextBlock Text="{Binding EvaluationText}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Kilépés -->
        <Button Content="Kilépés"
                Command="{Binding ExitCommand}"
                Grid.Row="4"
                Grid.ColumnSpan="2"
                Width="100"
                Height="30"
                Margin="5"
                HorizontalAlignment="Right"/>

    </Grid>
</Window>