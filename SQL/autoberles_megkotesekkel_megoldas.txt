-- 1.
CREATE DATABASE autoberles13b DEFAULT CHARACTER SET utf8 COLLATE utf8_hungarian_ci;

-- 2.
CREATE TABLE berlok
(
    id int AUTO_INCREMENT PRIMARY KEY,
    nev varchar(100) NOT null,
    jogositvany varchar(15) NOT null,
    telefonszam varchar(20)
);

CREATE TABLE autok
(
    id int AUTO_INCREMENT PRIMARY KEY,
    rendszam varchar(6) NOT null UNIQUE,
    tipus varchar(100) NOT null,
    evjarat SMALLINT,
    szin varchar(30)
);

CREATE TABLE kolcsonzes
(
    id int AUTO_INCREMENT PRIMARY KEY,
    berloid int,
    autoid int,
    berletkezdete date NOT null,
    napokszama tinyint,
    napidij int NOT null,
    FOREIGN KEY(berloid) REFERENCES berlok(id),
    FOREIGN KEY(autoid) REFERENCES autok(id)
);

-- 3.
INSERT INTO `autok`(`id`, `rendszam`, `tipus`, `evjarat`, `szin`) VALUES 
(null,"ABC456","Ford Ka",2003,"Pink"),
(null,"ABC123","Volkswagen Golf",2011,"Fehér"),
(null,"ABC157","Ford Mondeo",2015,"Fekete"),
(null,"ABC448","Volkswagen Golf",2012,"Kék");


-- 4.
INSERT INTO `berlok`(`id`, `nev`, `jogositvany`, `telefonszam`) VALUES 
(null,"Kandúr Károly","LR337157","06-41-334112"),
(null,"Gipsz Jakab","VE4455112","06-41-555223");

-- 5.
INSERT INTO `kolcsonzes`(`id`, `berloid`, `autoid`, `berletkezdete`, `napokszama`, `napidij`) VALUES
(null,1,3,"2017-04-23",6,12500),
(null,2,2,"2017-04-25",null,9999);

-- 6.
ALTER TABLE berlok ADD INDEX jogositvany_idx(jogositvany);

-- vagy
CREATE INDEX jogositvany_idx ON berlok(jogositvany);

-- 7.
ALTER TABLE autok ADD UNIQUE INDEX rendszam_idx(rendszam);

-- vagy
CREATE UNIQUE INDEX rendszam_idx ON autok(rendszam);

-- 8.
ALTER TABLE autok DROP INDEX rendszam;

-- vagy
DROP INDEX rendszam ON autok;

-- 9.
ALTER TABLE autok MODIFY evjarat int;

-- 10.
ALTER TABLE autok ADD veteliar bigint;

-- 11.
UPDATE autok SET veteliar = 5000000 WHERE tipus LIKE "Ford%";
UPDATE autok SET veteliar = 4300000 WHERE tipus LIKE "Volkswagen%";

-- 12.
ALTER TABLE autok ADD afa double;

-- 13.
UPDATE autok SET afa = veteliar * 0.27;

-- 14.
DELETE FROM autok WHERE szin = "fehér";
-- megjegyzés: ez a parancs nem fut le, mert pont a fehér autót bérelte valaki, az idegen kulcsra vonatkozó megkötés miatt a törlés nem engedélyezett

DELETE FROM autok WHERE szin = "pink";
-- megjegyzés: ez a parancs már lefut, mert nincs kapcsolódó rekord a másik táblában

-- 15.
ALTER TABLE berlok DROP telefonszam;
